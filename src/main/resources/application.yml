server:
#  port: 8010 # ?? ?? ?? (??? 8080)

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/sbbprac?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: lion
    password: Lion1234!@
  thymeleaf:
    cache: false # ???? ?? ??
    prefix: file:src/main/resources/templates/ # ???? ?? ??(? ??? ?? ??)
    devtools:
      livereload:
        enabled: true
      restart:
        enabled: true
  jpa:
    hibernate:
      ddl-auto: update # DB ??? ????(??? ???? ??? ?)
    properties:
      hibernate:
        show_sql: true
        format_sql: true


